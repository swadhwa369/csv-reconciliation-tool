# CSV Reconciliation Tool - Environment Configuration Example
# Copy this file to .env and update the values for your deployment

# Public URL for the application (used in docs, emails, etc.)
APP_PUBLIC_URL=https://reconcile.opslab.app

# CORS allowed origins (comma-separated)
# Include your custom domain, Heroku domain, and localhost for development
ALLOW_ORIGINS=https://reconcile.opslab.app,https://your-app-name.herokuapp.com,http://localhost:3000,http://localhost:8000

# Environment identifier
ENVIRONMENT=production

# Optional: Custom session cleanup interval (hours)
# SESSION_CLEANUP_HOURS=6

# Optional: Override max file size or row limits
# MAX_FILE_SIZE=10485760  # 10MB in bytes
# MAX_ROWS=100000
